export const secretsProd = {
  pg_host: 'postgres',
  pg_user: 'postgres',
  pg_password: 'password',

  redis_host: 'redis',
  redis_password: null,

  cf_auth_headers: {
    'X-Auth-Email': null,
    'X-Auth-Key': null
  }
};
